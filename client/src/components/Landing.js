import React from 'react';

const Landing = () => {
    return (
        <div style={{textAlign: 'center'}}>
            <h1>
                Email Survey Web Application
            </h1>
        </div>
    );
};

export default Landing;
